/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  output: 'export',
  basePath: '/area-shape-homework',
  assetPrefix: '/area-shape-homework',
  images: {
    unoptimized: true
  }
}

module.exports = nextConfig 